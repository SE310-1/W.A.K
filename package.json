{
  "name": "W.A.K",
  "version": "1.0.0",
  "description": "The W.A.K project",
  "scripts": {
    "removeModules": "rm -rf node_modules && cd client && rm -rf node_modules && cd ../server && rm -rf node_modules",
    "installAll": "npm install && cd client && npm install && cd ../server && npm install",
    "tsc": "tsc",
    "tsc:w": "tsc -w"
  },
  "dependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "^20.7.0",
    "@types/react": "^18.2.22",
    "@types/react-dom": "^18.2.7",
    "react-loader-spinner": "^5.4.5",
    "react-social-login-buttons": "^3.9.1",
    "reactjs-social-login": "^2.6.3",
    "typescript": "^5.2.2"
  }
}
